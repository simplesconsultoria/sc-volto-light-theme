Added a machine-extracted design system reference under `docs/design-system/`: an inventory of every block, listing variation, theme mapping and custom property with its resolved light and dark value; a gap analysis carrying a full WCAG contrast audit of the shipped palette; and self-contained preview pages for the colour system, the block themes and the type scale, marked up for import into Claude Design. @ericof
