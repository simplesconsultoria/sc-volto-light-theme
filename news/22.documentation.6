Reconciled the design-system documentation with the palette and theme-schema changes: the contrast section now records both WCAG failures as fixed and what fixed them, the per-project overlay section describes the 38 `_light`/`_dark` colour fields the control panel actually serves, and the content-type colours, base ramp and type scale carry their current values. `GAPS.md` was renumbered when its resolved sections were removed, so every cross-reference in `DESIGN_SYSTEM.md`, `INVENTORY.md` and the preview pages was re-pointed; the identical `narrow` and `default` container widths were restored as an open gap, having been removed while still true. @ericof
