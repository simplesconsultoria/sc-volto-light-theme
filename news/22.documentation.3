Rewrote the design system manifest from verified evidence and moved it under `docs/design-system/`, leaving a short pointer at the repository root. The previous draft carried 69 unverified claims and 51 open items, and re-documented the schemas of blocks belonging to dependencies; the manifest now covers what the add-on owns, states the rules that govern how it composes with the layers beneath it, and points at the runtime inventory rather than repeating a list that goes stale. @ericof
