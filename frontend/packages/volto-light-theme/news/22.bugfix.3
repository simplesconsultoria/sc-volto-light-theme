Fixed the Hero block's `fullWidth` option being unreachable. Both views branch on it to render full-bleed when there is no image, but it appeared in no fieldset, so no editor could set it. It is now part of the Layout fieldset. Also corrected a `--theme-low-contrast-foreground` reference in `_heroBlock.scss` that was missing its `-color` suffix and silently dropped the colour. @ericof
