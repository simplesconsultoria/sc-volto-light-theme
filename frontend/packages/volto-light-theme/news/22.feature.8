Added separator block stories covering its three controls — block width, the short-line toggle and alignment. Alignment only applies when `shortLine` is on, which its schema enforces by disabling the control otherwise, so the stories pair them rather than offering alignment on a full-width rule. @ericof
