export type SCVLTSettings = {
  headerBar: {
    display: boolean;
    elements: {
      accessibilityControls: boolean;
      languageSelector: boolean;
      themeToggle: boolean;
      userTools: boolean;
    };
  };
};
