/**
 * Action types owned by this add-on.
 *
 * Kept separate from Volto's own `constants/ActionTypes` so the names cannot
 * collide with a future core action.
 */

export const GET_INHERIT = 'GET_INHERIT';
